sudo: false
language: node_js
node_js:
  - "stable"

cache:
  directories:
    - node_modules

install:
  - npm install
  - npm install -g codecov mocha istanbul

script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R tests/*.test.js
  - codecov